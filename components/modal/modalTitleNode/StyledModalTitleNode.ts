import styled from 'styled-components';

const StyledModalTitleNode = styled.div`
  display: flex;
  flex-direction: row;
  justify-content: space-between;

  span {
    font-size: 0.9rem;
    color: #989898;
  }

  button {
    align-self: center;
  }
`;

export default StyledModalTitleNode;
