import styled from 'styled-components';

const StyledUpdateTitleInputs = styled.div`
  display: flex;
  max-width: 100%;
  justify-content: space-between;

  svg {
    font-size: 16px;
  }

  textarea {
    font-size: inherit;
    font-family: inherit;
    padding: 5px;
    border: none;
    outline: #4153AF;
  }
`;

export default StyledUpdateTitleInputs;
