import styled from 'styled-components';

const StyledTaskList = styled.div`
  max-height: 100%;
  min-height: 200px;
  display: flex;
  flex-flow: column;
  gap: 10px;
  overflow-y: scroll;
`;

export default StyledTaskList;
